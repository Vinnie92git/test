server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Importante para SPA: cualquier ruta devuelve index.html
    location / {
        try_files $uri /index.html;
    }
}
